(function () {
    'use strict';

    var radius = 12;  // cm
    const pi = 3.14;  // circle ratio
    var area = radius * radius * pi;  // Formula of circle area: (radius)^2 * pi
    document.write(area);
})();